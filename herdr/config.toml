[theme]
name = "tokyo-night"
auto_switch = false

[ui.toast]
delivery = "system"

[ui]
show_agent_labels_on_pane_borders = true

# alt+k: collapse/expand the pi agent pane (see herdr/pi-toggle.sh).
# Collapse parks pi in its own tab (agent keeps running); anywhere else the
# chord is forwarded to the focused pane, so nvim's <M-k> handler still works.
[[keys.command]]
key = "alt+k"
type = "shell"
command = "$HOME/dotfiles/herdr/pi-toggle.sh"
description = "toggle pi panel"
